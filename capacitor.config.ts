import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'lenguaje5-crud',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
